# 검색 기능 구현 계획 (4주차 A)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ROADMAP 4주차 A — 사이트 헤더 검색 아이콘 + Cmd+K 단축키로 빠른 노드 검색. 빌드 시점에 content 디렉터리에서 MiniSearch 인덱스 생성, 클라이언트 lazy load, 한국어 2-글자 n-gram 부분 매칭.

**Architecture:** content/\*.mdx → 빌드 시점 MiniSearch 인덱스 → `public/search-index.json` 정적 자산 → 첫 모달 열기 시 fetch + MiniSearch.loadJSON. shadcn `Command`(CMDK)로 모달·키보드 처리, 진입은 헤더 아이콘 + Cmd/Ctrl+K.

**Tech Stack:** Next.js 16 App Router · TypeScript · `minisearch` · `cmdk`(shadcn `Command`) · Tailwind

**참조:** `docs/superpowers/specs/2026-05-09-search-design.md`

**브랜치 전략:** `feat/search` 브랜치에서 작업, task별 commit, 마지막에 PR 생성.

---

## 사전 작업: 새 브랜치 + main 동기화

- [ ] **main 최신화 + 새 브랜치**

```bash
git checkout main && git pull origin main && git checkout -b feat/search
```

---

## 파일 구조

### 신규 파일

| 파일                                           | 책임                                                |
| ---------------------------------------------- | --------------------------------------------------- |
| `scripts/build-search-index.ts`                | content 읽기 + MiniSearch 인덱스 생성 → JSON        |
| `src/lib/search/tokenize.ts`                   | 한국어 2-글자 n-gram + 영문 단어 토크나이저         |
| `src/lib/search/index.ts`                      | MiniSearch 설정·loadFromJson 헬퍼 (서버·클라 공통)  |
| `src/components/search/use-cmdk.ts`            | Cmd+K / Ctrl+K 단축키 훅                            |
| `src/components/search/search-empty-state.tsx` | 빈 상태 (도메인당 1개 추천 6개)                     |
| `src/components/search/search-result-item.tsx` | 결과 카드 (도메인 라벨 + 제목 + snippet)            |
| `src/components/search/search-dialog.tsx`      | shadcn Command 모달 + lazy index load + search 동작 |
| `src/components/search/search-trigger.tsx`     | 헤더 검색 아이콘 버튼 + dialog 토글                 |

### 수정 파일

| 파일                                     | 변경 종류                                                |
| ---------------------------------------- | -------------------------------------------------------- |
| `package.json`                           | `minisearch` 의존성 + `postbuild` script                 |
| `.gitignore`                             | `public/search-index.json` 추가                          |
| `src/components/ui/command.tsx` (shadcn) | shadcn add로 자동 생성 (cmdk 포함)                       |
| `src/components/ui/dialog.tsx` (shadcn)  | shadcn add로 자동 생성                                   |
| `src/components/layout/site-header.tsx`  | `<SearchTrigger />` 박기                                 |
| `docs/ROADMAP.md`                        | Pagefind → MiniSearch 갱신 + 4주차 A 항목 체크 (구현 후) |
| `CLAUDE.md`                              | 현재 진행 상황 갱신 (구현 후)                            |

### 빌드 산출물

| 파일                       | 비고                               |
| -------------------------- | ---------------------------------- |
| `public/search-index.json` | gitignore, postbuild에서 매번 갱신 |

---

## Task 1: shadcn Command + Dialog + minisearch 설치

**Files:**

- Create (auto): `src/components/ui/command.tsx`, `src/components/ui/dialog.tsx`
- Modify: `package.json`, `pnpm-lock.yaml`

- [ ] **Step 1: shadcn 컴포넌트 추가**

```bash
pnpm dlx shadcn@latest add command dialog
```

- [ ] **Step 2: minisearch 설치**

```bash
pnpm add minisearch
```

- [ ] **Step 3: typecheck**

```bash
pnpm typecheck
```

Expected: 통과 (shadcn 추가 후 cmdk·radix-ui 자동 포함됨).

- [ ] **Step 4: commit**

```bash
git add src/components/ui/command.tsx src/components/ui/dialog.tsx package.json pnpm-lock.yaml
git commit -m "$(cat <<'EOF'
chore(search): shadcn Command·Dialog + minisearch 설치

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: 한국어 토크나이저

**Files:**

- Create: `src/lib/search/tokenize.ts`

- [ ] **Step 1: 작성**

```typescript
export function koreanNgramTokenize(text: string): string[] {
  const tokens: string[] = [];
  const words = text.split(/[\s,.!?:;()[\]{}<>"'`/\\|@#$%^&*+=~\-—–]+/).filter(Boolean);
  for (const word of words) {
    if (/^[a-zA-Z0-9_]+$/.test(word)) {
      tokens.push(word.toLowerCase());
      continue;
    }
    if (word.length === 1) {
      tokens.push(word.toLowerCase());
      continue;
    }
    for (let i = 0; i < word.length - 1; i++) {
      tokens.push(word.slice(i, i + 2).toLowerCase());
    }
  }
  return tokens;
}
```

- [ ] **Step 2: typecheck**

```bash
pnpm typecheck
```

- [ ] **Step 3: commit**

```bash
git add src/lib/search/tokenize.ts
git commit -m "feat(search): 한국어 2-글자 n-gram 토크나이저"
```

---

## Task 3: MiniSearch 설정 헬퍼

**Files:**

- Create: `src/lib/search/index.ts`

- [ ] **Step 1: 작성**

```typescript
import MiniSearch, { type Options } from 'minisearch';

import { koreanNgramTokenize } from './tokenize';

export type SearchDoc = {
  id: string;
  domain: string;
  slug: string;
  title: string;
  summary: string;
  body: string;
  tags: string[];
};

export const SEARCH_OPTIONS: Options<SearchDoc> = {
  fields: ['title', 'summary', 'tags', 'body'],
  storeFields: ['id', 'domain', 'slug', 'title', 'summary', 'body'],
  tokenize: koreanNgramTokenize,
  processTerm: (term) => term.toLowerCase().trim() || null,
  searchOptions: {
    boost: { title: 3, summary: 2, tags: 2, body: 1 },
    prefix: true,
    fuzzy: 0.1,
  },
};

export function createSearchIndex(): MiniSearch<SearchDoc> {
  return new MiniSearch<SearchDoc>(SEARCH_OPTIONS);
}

export function loadSearchIndex(json: string): MiniSearch<SearchDoc> {
  return MiniSearch.loadJSON<SearchDoc>(json, SEARCH_OPTIONS);
}
```

- [ ] **Step 2: typecheck**

```bash
pnpm typecheck
```

- [ ] **Step 3: commit**

```bash
git add src/lib/search/index.ts
git commit -m "feat(search): MiniSearch 설정·인덱스 생성/로드 헬퍼"
```

---

## Task 4: 빌드 인덱스 스크립트

**Files:**

- Create: `scripts/build-search-index.ts`

- [ ] **Step 1: 작성**

```typescript
import { promises as fs } from 'node:fs';
import path from 'node:path';

import { listAllNodes } from '@/lib/content/loader';
import { createSearchIndex, type SearchDoc } from '@/lib/search/index';

async function main() {
  const all = await listAllNodes();
  const published = all.filter((n) => n.frontmatter.status === 'published');

  const docs: SearchDoc[] = await Promise.all(
    published.map(async (n) => {
      const filePath = path.join(process.cwd(), 'content', n.domain, n.filename);
      const raw = await fs.readFile(filePath, 'utf8');
      const bodyStart = raw.indexOf('\n---', 4);
      const body = bodyStart === -1 ? '' : raw.slice(bodyStart + 4).trim();
      return {
        id: n.frontmatter.id,
        domain: n.domain,
        slug: n.slug,
        title: n.frontmatter.title,
        summary: n.frontmatter.summary,
        body,
        tags: n.frontmatter.tags ?? [],
      };
    }),
  );

  const index = createSearchIndex();
  index.addAll(docs);

  const outPath = path.join(process.cwd(), 'public', 'search-index.json');
  await fs.writeFile(outPath, JSON.stringify(index), 'utf8');

  const stats = await fs.stat(outPath);
  console.log(
    `[search] indexed ${docs.length} docs → ${outPath} (${(stats.size / 1024).toFixed(1)}KB)`,
  );
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
```

- [ ] **Step 2: `package.json`에 postbuild script 추가**

기존 `prebuild`가 `pnpm content:check`. 그 옆에 추가:

```json
{
  "scripts": {
    "postbuild": "tsx scripts/build-search-index.ts"
  }
}
```

`tsx`가 없을 가능성: `pnpm exec tsx --version` 또는 `pnpm list tsx`로 확인. 없으면 `pnpm add -D tsx` 추가.

- [ ] **Step 3: `.gitignore`에 인덱스 추가**

```bash
echo "" >> .gitignore && echo "# build-time search index" >> .gitignore && echo "public/search-index.json" >> .gitignore
```

- [ ] **Step 4: 빌드 실행 + 인덱스 생성 확인**

```bash
pnpm build
ls -la public/search-index.json
```

Expected: `[search] indexed 6 docs → ...` 로그 + 파일 생성 (1MB 미만).

- [ ] **Step 5: commit**

```bash
git add scripts/build-search-index.ts package.json pnpm-lock.yaml .gitignore
git commit -m "$(cat <<'EOF'
feat(search): postbuild 인덱스 생성 스크립트

content/*.mdx 전체 읽어 MiniSearch 인덱스 → public/search-index.json.
gitignore에 추가, 매 빌드마다 갱신.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: Cmd+K 단축키 훅

**Files:**

- Create: `src/components/search/use-cmdk.ts`

- [ ] **Step 1: 작성**

```typescript
'use client';

import { useEffect } from 'react';

export function useCmdK(onTrigger: () => void) {
  useEffect(() => {
    function onKeyDown(e: KeyboardEvent) {
      if (e.key === 'k' && (e.metaKey || e.ctrlKey)) {
        e.preventDefault();
        onTrigger();
      }
    }
    window.addEventListener('keydown', onKeyDown);
    return () => window.removeEventListener('keydown', onKeyDown);
  }, [onTrigger]);
}
```

- [ ] **Step 2: typecheck + commit**

```bash
pnpm typecheck
git add src/components/search/use-cmdk.ts
git commit -m "feat(search): Cmd+K / Ctrl+K 단축키 훅"
```

---

## Task 6: SearchEmptyState

**Files:**

- Create: `src/components/search/search-empty-state.tsx`

- [ ] **Step 1: 작성**

```tsx
'use client';

import type { NodeMeta } from '@/lib/content/loader';
import { DOMAIN_META } from '@/lib/content/schema';

import { SearchResultItem } from './search-result-item';

export function SearchEmptyState({
  nodes,
  onSelect,
}: {
  nodes: NodeMeta[];
  onSelect: (n: NodeMeta) => void;
}) {
  const recommended = Object.keys(DOMAIN_META).flatMap((domain) => {
    const first = nodes
      .filter((n) => n.domain === domain)
      .sort((a, b) => a.frontmatter.order - b.frontmatter.order)[0];
    return first ? [first] : [];
  });

  return (
    <div className="space-y-1 p-2">
      <p className="text-muted-foreground px-2 py-1 text-xs">도메인별 시작 노드</p>
      {recommended.map((n) => (
        <SearchResultItem
          key={n.frontmatter.id}
          node={n}
          snippet={null}
          onSelect={() => onSelect(n)}
        />
      ))}
    </div>
  );
}
```

- [ ] **Step 2: typecheck + commit**

```bash
pnpm typecheck
git add src/components/search/search-empty-state.tsx
git commit -m "feat(search): 빈 상태 컴포넌트 (도메인당 1개 추천 6개)"
```

---

## Task 7: SearchResultItem

**Files:**

- Create: `src/components/search/search-result-item.tsx`

- [ ] **Step 1: 작성**

```tsx
'use client';

import type { NodeMeta } from '@/lib/content/loader';
import { DOMAIN_META } from '@/lib/content/schema';
import { CommandItem } from '@/components/ui/command';

export function SearchResultItem({
  node,
  snippet,
  onSelect,
}: {
  node: NodeMeta;
  snippet: string | null;
  onSelect: () => void;
}) {
  const domainLabel = DOMAIN_META[node.domain].label;
  return (
    <CommandItem onSelect={onSelect} className="flex flex-col items-start gap-1 px-3 py-2">
      <div className="flex w-full items-center gap-2">
        <span className="text-muted-foreground text-xs">{domainLabel}</span>
        <span className="text-sm font-medium">{node.frontmatter.title}</span>
      </div>
      <p className="text-muted-foreground line-clamp-2 text-xs">
        {snippet ?? node.frontmatter.summary}
      </p>
    </CommandItem>
  );
}
```

- [ ] **Step 2: typecheck + commit**

```bash
pnpm typecheck
git add src/components/search/search-result-item.tsx
git commit -m "feat(search): 검색 결과 카드 (도메인 라벨 + 제목 + snippet)"
```

---

## Task 8: SearchDialog (핵심)

**Files:**

- Create: `src/components/search/search-dialog.tsx`

이 컴포넌트는 다른 task들과 독립적이지 않음 — 4·5·6·7 결과 사용. 전제: 그 컴포넌트들이 다 commit됐음.

- [ ] **Step 1: 작성**

```tsx
'use client';

import { useEffect, useRef, useState } from 'react';
import { useRouter } from 'next/navigation';

import { CommandDialog, CommandEmpty, CommandInput, CommandList } from '@/components/ui/command';
import type { NodeMeta } from '@/lib/content/loader';
import { loadSearchIndex, type SearchDoc } from '@/lib/search/index';

import { SearchEmptyState } from './search-empty-state';
import { SearchResultItem } from './search-result-item';
import type MiniSearch from 'minisearch';

const MAX_RESULTS = 10;

export function SearchDialog({
  open,
  onOpenChange,
  nodes,
}: {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  nodes: NodeMeta[];
}) {
  const router = useRouter();
  const [query, setQuery] = useState('');
  const indexRef = useRef<MiniSearch<SearchDoc> | null>(null);
  const [indexReady, setIndexReady] = useState(false);
  const [results, setResults] = useState<{ doc: SearchDoc; snippet: string | null }[]>([]);

  useEffect(() => {
    if (!open || indexRef.current) return;
    let cancelled = false;
    fetch('/search-index.json')
      .then((r) => r.text())
      .then((json) => {
        if (cancelled) return;
        indexRef.current = loadSearchIndex(json);
        setIndexReady(true);
      })
      .catch((err) => console.error('[search] index load failed', err));
    return () => {
      cancelled = true;
    };
  }, [open]);

  useEffect(() => {
    if (!indexRef.current || query.trim().length === 0) {
      setResults([]);
      return;
    }
    const raw = indexRef.current.search(query, { ...indexRef.current.searchOptions });
    const top = raw.slice(0, MAX_RESULTS);
    const next = top.map((r) => {
      const doc = r as unknown as SearchDoc;
      const snippet = extractSnippet(doc.body, query, 120);
      return { doc, snippet };
    });
    setResults(next);
  }, [query, indexReady]);

  function selectByDoc(doc: SearchDoc) {
    onOpenChange(false);
    setQuery('');
    router.push(`/learn/${doc.domain}/${doc.slug}`);
  }

  function selectByNode(n: NodeMeta) {
    onOpenChange(false);
    setQuery('');
    router.push(`/learn/${n.domain}/${n.slug}`);
  }

  return (
    <CommandDialog open={open} onOpenChange={onOpenChange}>
      <CommandInput placeholder="노드·키워드 검색…" value={query} onValueChange={setQuery} />
      <CommandList>
        {query.trim().length === 0 ? (
          <SearchEmptyState nodes={nodes} onSelect={selectByNode} />
        ) : results.length === 0 ? (
          <CommandEmpty>검색 결과 없음</CommandEmpty>
        ) : (
          <div className="space-y-1 p-2">
            {results.map(({ doc, snippet }) => {
              const node = nodes.find((n) => n.frontmatter.id === doc.id);
              if (!node) return null;
              return (
                <SearchResultItem
                  key={doc.id}
                  node={node}
                  snippet={snippet}
                  onSelect={() => selectByDoc(doc)}
                />
              );
            })}
          </div>
        )}
      </CommandList>
    </CommandDialog>
  );
}

function extractSnippet(body: string, query: string, len: number): string | null {
  const q = query.trim().toLowerCase();
  if (!q) return null;
  const idx = body.toLowerCase().indexOf(q);
  if (idx === -1) return body.slice(0, len);
  const start = Math.max(0, idx - 30);
  const end = Math.min(body.length, idx + q.length + len - 30);
  return (start > 0 ? '… ' : '') + body.slice(start, end) + (end < body.length ? ' …' : '');
}
```

- [ ] **Step 2: typecheck**

```bash
pnpm typecheck
```

만약 `MiniSearch.searchOptions` 접근이 오류면, options 인자 생략 (`indexRef.current.search(query)`)으로 수정.

- [ ] **Step 3: commit**

```bash
git add src/components/search/search-dialog.tsx
git commit -m "$(cat <<'EOF'
feat(search): SearchDialog — 모달 + lazy 인덱스 + 검색·결과·빈 상태 통합

shadcn CommandDialog 기반. 첫 open 시 /search-index.json fetch + MiniSearch.loadJSON으로 클라 인스턴스 복원.
검색 결과 max 10개, snippet 자동 추출.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: SearchTrigger (헤더 아이콘)

**Files:**

- Create: `src/components/search/search-trigger.tsx`

- [ ] **Step 1: 작성**

```tsx
'use client';

import { Search } from 'lucide-react';
import { useState } from 'react';

import { Button } from '@/components/ui/button';
import type { NodeMeta } from '@/lib/content/loader';

import { SearchDialog } from './search-dialog';
import { useCmdK } from './use-cmdk';

export function SearchTrigger({ nodes }: { nodes: NodeMeta[] }) {
  const [open, setOpen] = useState(false);

  useCmdK(() => setOpen((v) => !v));

  return (
    <>
      <Button
        type="button"
        variant="ghost"
        size="icon"
        aria-label="검색 (Cmd+K)"
        onClick={() => setOpen(true)}
      >
        <Search className="h-4 w-4" />
      </Button>
      <SearchDialog open={open} onOpenChange={setOpen} nodes={nodes} />
    </>
  );
}
```

`lucide-react`가 이미 설치돼있다고 가정. 없으면 Step 0에서 `pnpm add lucide-react`.

- [ ] **Step 2: typecheck + commit**

```bash
pnpm typecheck
git add src/components/search/search-trigger.tsx
git commit -m "feat(search): 헤더 검색 아이콘 트리거 + Cmd+K"
```

---

## Task 10: SiteHeader에 SearchTrigger 박기

**Files:**

- Modify: `src/components/layout/site-header.tsx`

이건 서버 컴포넌트일 가능성 큼. `listPublishedNodes()`로 nodes를 fetch해서 client `<SearchTrigger>`에 props로 넘김.

- [ ] **Step 1: 현재 site-header.tsx 확인**

```bash
cat src/components/layout/site-header.tsx
```

서버/클라 여부 확인. 만약 client component면 SearchTrigger 자체에서 fetch가 어려우니, 새 서버 wrapper를 만드는 방향:

- `site-header-search.tsx` (server) — `listPublishedNodes()` 호출 → `<SearchTrigger nodes={...} />` 렌더
- `site-header.tsx`에서 `<SiteHeaderSearch />` import + 우측 영역에 박기

- [ ] **Step 2: 우측 영역(theme-toggle 옆)에 SearchTrigger 박기**

서버 컴포넌트 케이스 — 직접 박음:

```tsx
import { SearchTrigger } from '@/components/search/search-trigger';
import { listPublishedNodes } from '@/lib/content/loader';

// ... 헤더 함수 안:
const nodes = await listPublishedNodes();

// JSX 우측 영역:
<SearchTrigger nodes={nodes} />;
```

기존 헤더가 async가 아니면 async로 변경.

- [ ] **Step 3: typecheck + 빌드**

```bash
pnpm typecheck && pnpm build
```

- [ ] **Step 4: 로컬 dev 시각 확인**

```bash
pnpm dev
```

- 헤더에 검색 아이콘 표시
- 클릭 → 모달 열리고 빈 상태 6개 노드
- Cmd+K → 모달 토글
- 한국어 키워드 입력 (예: "렌더") → 결과 표시 + snippet
- 결과 클릭 → 노드 페이지로 이동, 모달 닫힘

- [ ] **Step 5: commit**

```bash
git add src/components/layout/site-header.tsx
git commit -m "feat(search): 헤더에 검색 트리거 wire"
```

---

## Task 11: 문서 갱신

**Files:**

- Modify: `docs/ROADMAP.md`, `CLAUDE.md`

- [ ] **Step 1: ROADMAP 4주차 항목 갱신**

```markdown
## 4주차: 검색 + 노드 페이지 마감

- [x] 자체 인덱싱 (MiniSearch + 한국어 n-gram) — Pagefind 거절, dynamic 라우트 비호환
- [x] 검색 UI (Cmd+K, shadcn Command)
- [x] 검색 결과 → 노드 페이지 이동
- [ ] 코드 블록 복사 버튼
- [ ] 헤더 앵커 + 목차 (TOC, 우측)
- [x] 다음/이전 노드 네비
- [ ] 다크/라이트 일관성 검증
- [ ] PWA 매니페스트 + 오프라인 캐싱 (next-pwa 또는 Workbox)
```

- [ ] **Step 2: CLAUDE.md 안티패턴 + 진행 상황 갱신**

`Pagefind` 관련 안티패턴 이미 있다면 거절 사유 갱신. 없으면 그대로.

"현재 진행 상황":

```markdown
## 현재 진행 상황

- **단계**: 4주차 B(노드 페이지 마감) 직전
- **방금 완료**: 검색 기능(`docs/superpowers/specs/2026-05-09-search-design.md`)
```

- [ ] **Step 3: commit**

```bash
git add docs/ROADMAP.md CLAUDE.md
git commit -m "docs: 4주차 A 검색 완료 → B 시작 표기"
```

---

## Task 12: PR 생성

- [ ] **Step 1: push + PR**

```bash
git push -u origin feat/search
gh pr create --title "feat: 4주차 A — 검색 기능 (MiniSearch + Cmd+K)" --body "$(cat <<'EOF'
## Summary
- 헤더 검색 아이콘 + Cmd+K(맥) / Ctrl+K(윈도우) 단축키
- MiniSearch 자체 인덱싱 (Pagefind 거절: dynamic 노드 페이지 비호환)
- 한국어 2-글자 n-gram 토크나이저로 부분 매칭
- 빈 상태 = 도메인당 1개 추천 6개
- 검색 결과 = 단일 리스트 + 도메인 라벨 + body snippet

## Spec / Plan
- `docs/superpowers/specs/2026-05-09-search-design.md`
- `docs/superpowers/plans/2026-05-09-search.md`

## 빌드 산출물
- `public/search-index.json` (gitignore) — postbuild에서 매번 갱신

## Test plan
- [ ] Cmd+K (맥) / Ctrl+K (윈도우) → 모달 열림
- [ ] 헤더 검색 아이콘 클릭 → 모달 열림 (모바일 포함)
- [ ] 빈 상태 → 6개 도메인 추천 노드 노출
- [ ] "렌더" 입력 → "렌더링 파이프라인" 같은 부분 매칭 결과
- [ ] 결과 카드에 도메인 라벨·제목·body snippet 노출
- [ ] ↑↓로 결과 focus 이동, Enter로 노드 이동
- [ ] Esc로 모달 닫힘
- [ ] 결과 없음 시 "검색 결과 없음" 표시
- [ ] prod 배포 후 동일 시나리오 재확인 + 인덱스 파일 < 1.5MB

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

- [ ] **Step 2: PR URL 사용자에게 공유**

merge는 사용자가 직접.

---

## 자가 점검 체크리스트 (작성자용)

- ✅ Spec Q1~Q6 결정 모두 task에 매핑
- ✅ 12개 task로 분해, 각 task에 코드 박힘 (placeholder 없음)
- ✅ 타입 일관성 (`SearchDoc`, `MAX_RESULTS=10`, `SEARCH_OPTIONS`)
- ✅ TDD 미강요 (CLAUDE.md 컨벤션)
- ✅ feat 브랜치 + PR 워크플로
- ✅ 빌드 산출물 gitignore 처리
- ✅ MiniSearch.searchOptions 접근 fallback 명시 (Task 8 Step 2)
